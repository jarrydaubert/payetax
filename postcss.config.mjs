/**
 * PostCSS Configuration for Tailwind CSS v4
 * Uses ES modules format (.mjs) as recommended for v4
 *
 * Note: Tailwind v4 has built-in vendor prefixing - autoprefixer not needed
 */
export default {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};

# Backlog

Only open TODO items live here.

## Rules

- No status updates, progress notes, or completed work.
- Every item must include a concrete next step and a clear, testable Definition of Done.
- If an item changes behavior, its DoD must define regression protection: automated coverage at the right layer or an explicit replacement evidence path with a named bug target and oracle.
- When an item is complete, remove it from this file.

## Backlog Mantra

- Open TODO items only.
- Clear, testable DoD only.
- Behavior changes must say how they stay fixed:
  - automated coverage at the right layer, or
  - explicit replacement evidence path with a named bug target and oracle.

## Global DoD Contract (Applies To Every Item)

A backlog item is only complete when all conditions below are true:

1. `Outcome delivered`: the row's scope is implemented exactly as written.
2. `Regression protection defined`: behavior changes add or update automated coverage at the right layer, or explicitly justify why automation is the wrong layer and what evidence replaces it.
3. `Relevant validation passed`: the appropriate repo gates or targeted checks pass.
4. `Evidence captured`: the change set includes concrete proof for the completed outcome at the smallest sensible level (tests, screenshots, short notes, or docs).
5. `Backlog hygiene`: the completed item is removed from this file in the same change set.

If any condition is missing, the item remains open.

---

## P1 - Next

| ID | Work Item | Next Step | Definition of Done |
|---|---|---|---|
| `P1-32` | Stabilize GitLab to Vercel deployment status reconciliation | Reproduce the external-status failure path, then either fix the integration so successful production deploys consistently post a passing status or add a documented, scriptable recovery path that reconciles the commit status to the verified deployment. | A tested workflow exists for both success and failure cases, GitLab commit/pipeline status matches the verified Vercel deployment outcome for the latest tested commit, and the supported recovery path is documented in the ops runbook with concrete commands and evidence. |
| `P1-1` | Visual regression pilot | Restore or add the Playwright visual regression spec, then baseline homepage hero, calculator results, and Director Intelligence dashboard. | CI captures baselines for those three surfaces from an active checked-in test spec, fails on diffs for them, and the approve/update workflow is documented in the repo. |
| `P1-17` | Investigate low Google referrer share | Run a focused GSC + Ahrefs indexability audit (coverage, canonical, noindex, sitemap, robots, structured data) to understand why Bing/DDG/Yahoo outperform Google. | A short checked-in note captures the likely root causes, the evidence reviewed, and the smallest worthwhile follow-up fixes to try next. |
| `P1-18` | Strengthen internal links to `/best-for/*` landing pages | Audit incoming internal links for `/best-for/contractors`, `/best-for/first-job`, `/best-for/freelancers`, `/best-for/high-earners`, `/best-for/part-time`, `/best-for/pensioners`, `/best-for/scottish`, and `/best-for/students`, then add relevant contextual links from existing hubs, blog posts, tools, and other qualifying pages. | Each scoped `/best-for/*` page is linked from the `/best-for` hub and at least two additional relevant non-navigation internal pages, the added links are checked into page content/templates rather than relying only on shared header/footer chrome, and a fresh crawl/export evidence path no longer reports any scoped `/best-for/*` page as having only one incoming internal link. |
| `P1-27` | Track unresolved transitive dependency advisories | Keep the current upstream-transitive advisories (`minimatch`, `rollup`, `serialize-javascript`) documented in the allowlist with `via` chain and `lastChecked`, then re-run `bun run audit:deps` when runtime dependencies change or before a notable release. | The item closes only when `bun run audit:deps` no longer reports the currently tracked high-severity advisories, or any remaining accepted risk is documented with the responsible parent package/version and rationale rather than left implicit. |
| `P1-33` | Split calculator and director orchestration boundaries | Extract calculator and director store responsibilities into explicit domain services, input-normalization helpers, and side-effect adapters so Zustand stores stop owning validation, telemetry, persistence, and business orchestration at once. | `src/store/calculatorStore.ts` and `src/store/directorGuideStore.ts` become materially smaller adapter layers over documented modules, analytics/Sentry/persistence side effects are isolated behind narrow interfaces, current user-visible behavior is preserved, and regression protection covers at least one happy-path calculation flow plus one failure/edge path for each extracted boundary. |
| `P1-35` | Remove Americanisms from public site copy and lock UK style into content workflow | Run a sitewide audit for American spellings/terms across app, blog, metadata, and reusable marketing copy, then convert confirmed public-facing instances to UK usage and update the blog/content guidance to prevent regressions. | Public-facing copy no longer contains confirmed American spellings/terms where a UK equivalent is the house style, evergreen content guidance explicitly instructs authors to avoid Americanisms and prefer UK usage, and regression protection exists through a documented lint/check or a named periodic audit evidence path that covers blog plus key marketing surfaces. |
| `P1-36` | Decompose the PAYE calculation engine into smaller pure modules | Split `src/lib/taxCalculator.ts` into named pure modules for tax-code/allowance handling, taxable-pay normalization, income tax, NI, student loans, and result assembly while preserving `src/constants/taxRates.ts` as the single tax-rate source of truth. | `src/lib/taxCalculator.ts` is materially smaller and delegates to focused pure modules with explicit inputs/outputs, no tax figures are duplicated outside `src/constants/taxRates.ts`, current PAYE outputs remain unchanged for the existing oracle scenarios, and automated regression protection includes targeted module tests plus the existing calculator regression suites that cover HMRC verification, invariants, period reconciliation, and payslip regressions. |
| `P1-37` | Validate and simplify the homepage calculator loading path | Measure the current deferred calculator strategy on the homepage, decide whether to keep or reduce the deferral, and land the smallest simpler implementation that preserves discoverability, anchor navigation, and a strong LCP story. | A checked-in decision record names the baseline and compared variant, the homepage uses the simpler implementation unless measurements clearly justify the current one, `#tax-calculator` navigation still works on initial load and after hash changes, and regression protection covers homepage render plus calculator-anchor behavior with the smallest relevant automated test set and one documented measurement evidence path. |
| `P1-38` | Consolidate the validation surface and remove ambiguous imports | Replace the overlapping `@/lib/validation` surfaces with one canonical module structure, remove local tax-year schema duplication, and stop client code from depending on a broad umbrella validation export. | Tax-year and shared calculator validation come from one canonical export path, no client component imports the umbrella validation surface, duplicate validation declarations are removed rather than left in parallel, and targeted tests still prove valid/invalid handling at the app, store, and API boundaries that consume the moved schemas. |
| `P1-39` | Rationalize custom framework configuration | Audit `next.config.ts` customizations, remove the ones without a measured payoff or hard requirement, and document the rationale and verification path for the remainder. | `next.config.ts` is materially smaller or clearer, every remaining non-default customization has a checked-in rationale tied to a hard requirement or measured benefit, removed config does not regress build/runtime behavior, and the smallest relevant verification set passes for the touched surface (`bun run check:repo` plus targeted build/runtime evidence such as `bun run build`, route tests, or bundle analysis when the change claims a performance benefit). |
| `P1-40` | Remove Recharts and simplify calculator result visualization | Remove the current chart block from the landing-page calculator results, replace it with either no secondary visualization or a lighter static/HTML-first summary pattern, and then remove Recharts-specific code and config if nothing still needs it. | The landing-page calculator no longer renders the current Recharts chart section, result comprehension still depends on the table/cards rather than synthetic charts, no production file in `src/` imports from `recharts`, `package.json` and `next.config.ts` no longer reference Recharts, and targeted component/integration tests plus the smallest relevant build validation prove the calculator results flow still works after the removal. |
| `P1-41` | Unify the shipped design system and remove homepage-theme drift | Decide whether the Cyan/Emerald homepage direction is the canonical design system or a temporary layer, then collapse the duplicate variable sets into one theme-aware token contract used consistently by shipped UI. | The repo has one documented canonical token/theming contract for shipped surfaces, duplicate brand variables and near-duplicate surface/text variables are removed or explicitly aliased with rationale, homepage surfaces no longer depend on a parallel ad hoc theme layer, unused design-system variables such as `--bg-card-new` are removed or justified, and regression protection covers the touched homepage/shared UI surfaces at the component or page level. |

---

## P2 - Later (Planned Experiments)

| ID | Work Item | Next Step | Definition of Done |
|---|---|---|---|
| `P2-1` | React Compiler experiment | Enable in a controlled branch and compare INP/render metrics against baseline. | A keep or rollback decision is documented with before/after metrics, regression findings, and the exact branch/config tested. |
| `P2-2` | Cache Components / `use cache` experiment | Trial on static-heavy routes and capture TTFB/LCP impact. | Each tested route has a documented before/after measurement, a keep/rollback call, and a rollout plan only where the change is beneficial. |
| `P2-6` | Codify sitemap inclusion policy | Apply `docs/guides/SITEMAP_POLICY.md` to the current route families and record the include/exclude decisions in one checked-in note or script-backed source of truth. | The current route families have explicit sitemap include/exclude decisions with rationale, and sitemap selection no longer depends on memory or tribal knowledge. |
| `P2-7` | Run a manual accessibility sweep on core journeys | Perform a manual keyboard + screen-reader pass across the homepage calculator and Director Intelligence core flow. | One checked-in audit note captures what was tested, what failed, and whether each issue was fixed immediately or added back to the backlog with a concrete next step. |
| `P2-13` | Refresh the top organic blog posts | Select the 5 target posts using a documented recent organic-traffic source, refresh them, then submit the updated URLs to GSC indexing. | The 5 target posts are documented with the data source/date window used for selection, the refreshed URLs are shipped and submitted to GSC, and a short checked-in note records what changed and what to watch next. |
| `P2-15` | Decide low-value module test strategy explicitly | For thin wrappers (`safeStorage`, hooks, telemetry/security helpers), either add targeted tests or document why E2E/integration coverage is sufficient. | Each scoped module is listed in a checked-in decision record with either targeted tests or explicit rationale for relying on higher-level coverage, and no scoped module remains unlabeled or implicitly deferred. |
| `P2-18` | Move route-shell branching to App Router layouts | Replace pathname-based fullscreen branching with route groups or nested layouts so the shared shell can stay server-first. | Fullscreen routes use dedicated layout boundaries, the root layout no longer needs client pathname checks for shell selection, behavior matches the current UX, and regression tests cover shell selection on fullscreen and standard routes. |
| `P2-19` | Consolidate the UI import surface | Choose one canonical UI component path (`@/components/ui` or equivalent), codemod existing imports, and block mixed patterns from reappearing. | UI consumers import from one canonical path, legacy shim imports are removed or deprecated behind a lint-enforced rule, and no mixed import style remains in app code. |
| `P2-20` | Centralize site and SEO configuration | Move site URL, metadata defaults, verification codes, and sitemap selection policy into one typed configuration layer or versioned data source used by metadata, sitemap, and docs-driven workflows. | Metadata generation, sitemap logic, and site identity values read from one typed source of truth, duplicated hardcoded values are removed, targeted regression tests or scripted checks cover metadata/sitemap outputs, and SEO route-selection policy no longer lives only inside executable app logic. |
| `P2-21` | Simplify the analytics and consent integration surface | Introduce a single analytics gateway/consent orchestration layer so vendor adapters stay thin and page instrumentation logic is not embedded in one large client component. | Consent state has one source of truth, vendor adapters are isolated behind small modules, regression tests or scripted verification cover consent gating and core event routing, and current user-visible analytics behavior is preserved. |
| `P2-32` | Replace shipped raw palette classes with semantic design-system usage | Audit shipped component code for raw palette classes and convert recurring cases to semantic tokens, shared variants, or CSS-variable-backed utilities, keeping only narrow documented exceptions where embedded/third-party styling makes that unavoidable. | Shipped components no longer rely on ad hoc raw palette classes for recurring UI states where semantic tokens or shared variants should exist, remaining exceptions are documented with file-level rationale, and the repo has a repeatable verification path that catches reintroduction on app code rather than relying on tribal knowledge. |
| `P2-33` | Clean up design-token hygiene and low-value exports | Remove deprecated token exports, review low-signal utility mirrors such as zero-value spacing tokens, and add or formalize any missing brand-level semantic tokens needed by the chosen design-system direction. | Deprecated token exports such as `_DEPRECATED_BORDER_PRIMARY` are removed or fully isolated from public usage, low-value token wrappers are either pruned or explicitly justified in token docs, any retained brand palette has a canonical semantic token home, and affected consumers/tests/docs are updated so token usage stays intentional rather than additive drift. |
| `P2-34` | Rationalize component taxonomy and client boundaries in shared UI | Review obvious component-layer mismatches and top-level `use client` boundaries across shared UI so composition level and rendering mode are intentional rather than historical accidents. | Obvious mismatches such as overly complex atoms or trivial organism wrappers are either moved or explicitly documented as intentional exceptions, touched shared components keep only the `use client` directives they actually need, and regression tests cover any interactive behavior moved across server/client or component-layer boundaries. |
| `P2-26` | Shrink styling guidance to enforceable rules | Reduce `STYLING-GUIDELINES.md` to a shorter standard backed by the actual lint/audit rules and backlog exceptions rather than long-form prose. | The styling guide is materially shorter, references the enforcing scripts or rules for each hard constraint, and any remaining exceptions are tracked explicitly rather than hidden in narrative guidance. |
| `P2-30` | Align shared employee NI helpers with category-aware rates | Thread NI category through `src/lib/tax/employeeNI.ts` and the strategy/director helper paths that currently assume Category A so shared NI calculations are explicit about when A is intended. | Shared employee NI helpers accept an explicit NI category wherever category-specific behavior matters, any remaining Category A defaults are documented as intentional rather than implicit, and targeted tests cover at least one reduced-rate, exempt, and standard category path so a supplied category cannot be silently ignored. |
| `P2-31` | Upgrade verified major-version runtime dependencies with isolated validation | Upgrade `@vercel/analytics`, `@vercel/speed-insights`, and `shiki` one at a time with changelog review and repo validation after each step rather than as a bundle. | Each scoped major upgrade is landed with a short checked-in compatibility note, the app still builds and passes the smallest relevant test set after each individual upgrade, and any required code or config changes are captured in the same change set rather than deferred. |

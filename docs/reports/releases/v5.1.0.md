# Release Verification Report - v5.1.0

Date: 2026-02-20
Release Version: v5.1.0
Commit: be8314eb
Deployment URL: https://payetax.co.uk
Release Notes URL: docs/reports/releases/README.md#v510
Releasing Engineer: Jarry Daubert
Status: IN_PROGRESS

Reference checklist: `docs/guides/POST_RELEASE_VALIDATION.md`

Legend:
- `- [x]` complete
- `- [ ]` blocking and still open
- `- [~]` explicitly deferred/non-blocking with justification in `Notes`

---

## 0) Local Preflight

- [x] `bun run release:verify` passed
- [x] Key output/caveats captured below

Notes:
- 2026-03-11 17:26 UTC: `bun run release:verify` passed end-to-end after re-running the previously flaky `src/lib/security/__tests__/botGuard.test.ts` suite in isolation and then re-running the full release gate.
- `check:repo`, `audit:deps`, `test:no-coverage`, `next build --webpack`, and `bun run release:report:status` all completed successfully.
- `check-version` still warns that `package.json` remains at `v5.1.0` and the latest git tag is `v5.1.0`; this is informational for release hygiene and did not fail the gate.
- `src/lib/__tests__/rateLimit.test.ts` logs the expected test-environment fallback warning for missing `UPSTASH_REDIS_REST_URL/TOKEN`; the suite still passed and production distributed rate limiting has been separately verified below.
- 2026-03-10 10:46:42 UTC: attempted production rate-limit verification.
- `vercel env pull /tmp/payetax-rate-limit.env --environment=production --yes` succeeded, but the pulled production env did not include `RATE_LIMIT_HEALTH_SECRET` or any `UPSTASH_REDIS_REST_*` variables.
- `GET https://payetax.co.uk/api/ops/rate-limit-health` returned `503` with `{"error":"Rate-limit health check is not configured"}`.
- 2026-03-11 17:03:38 UTC: after setting `RATE_LIMIT_HEALTH_SECRET`, `UPSTASH_REDIS_REST_URL`, and `UPSTASH_REDIS_REST_TOKEN` in Vercel Production and redeploying, `GET /api/ops/rate-limit-health` with the secret header returned `200` and `{"ok":true,"diagnostics":{"configured":true,"backend":"distributed","upstashPing":"ok","lastFallbackReason":null,"lastFallbackAt":null}}`.
- 2026-03-11 17:04 UTC: `bun run ops:verify-rate-limit` passed against `https://payetax.co.uk` with output:
  - `configured: true`
  - `backend: distributed`
  - `upstashPing: ok`
  - `lastFallbackReason: none`
  - `lastFallbackAt: never`
  - `throttle statuses: 400, 400, 400, 400, 400, 429, 429`
- Result: distributed production rate limiting is verified and in-memory fallback is not active for the health-checked production backend.

## 1) Production Smoke

- [x] Homepage + calculator CTA path works
- [x] PAYE calculator works for at least two salaries
- [x] Director Intelligence flow completes
- [x] Blog index + one post loads cleanly

Notes:
- 2026-03-11: releasing engineer manually verified the live site and key production user flows, including homepage to calculator CTA path, multiple PAYE scenarios, Director Intelligence completion, and blog rendering.

## 2) Security + Abuse Controls

- [x] CSRF/origin enforcement verified
- [x] Rate limiting verified (distributed, not in-memory fallback)
  - Evidence: `bun run ops:verify-rate-limit` output captured below
- [x] IndexNow secret enforcement verified
- [x] Sentry webhook signature handling verified

Notes:
- 2026-03-11: live invalid-origin POST to `/api/newsletter/subscribe` returned `403 {"error":"Invalid request origin"}`.
- 2026-03-11: live invalid-secret POST to `/api/indexnow` returned `401 {"error":"Unauthorized"}`.
- 2026-03-11: pulled Vercel production env confirmed `SENTRY_WEBHOOK_SECRET` is set, while `INDEXNOW_SUBMIT_SECRET` is not currently present in production env.
- 2026-03-11: live invalid-signature POST to `/api/sentry-webhook` returned `401 {"error":"Invalid signature"}`.
- 2026-03-11: live valid signed POST to `/api/sentry-webhook` using the production secret returned `200 {"status":"ignored","reason":"Action: resolved"}`.
- Result: runtime abuse protections are active. IndexNow secret enforcement was verified on the rejection path; authenticated submission remains unavailable until `INDEXNOW_SUBMIT_SECRET` is configured in production.

## 3) Email + Webhooks

- [ ] Newsletter subscribe returns success and appears in Kit
- [ ] Unsubscribe token link returns success and the subscriber is unsubscribed or absent in Kit
- [x] PAYE results email route returns success and Resend records the outbound message
- [x] Director results email route returns success and Resend records the outbound message
- [~] Referral lead flow verified when enabled by the production env contract

Notes:
- 2026-03-21: live `POST /api/newsletter/subscribe` with browser-like headers and a fresh verification address returned `500 {"error":"Internal server error"}`.
- 2026-03-21: direct Kit API verification showed the configured production `KIT_FORM_ID` value (`648a4b276a`) is a form uid, while the current shipped helper posts to `/forms/<identifier>/subscribers` as though it were the numeric form id; the direct helper call failed with `Kit form subscribe failed (404)`.
- 2026-03-21: local patched helper resolved the uid to numeric form id `9084803` and subscribed successfully via Kit API. Release validation remains blocked until that fix is deployed and the live route returns success.
- 2026-03-21: live `POST /api/send-results` returned `200 {"success":true}` for the verification address, and Resend recorded outbound message `d195aab3-f68b-4cf1-9bad-77fd2083bc7b` with last event `bounced` because the disposable verification inbox does not exist.
- 2026-03-21: live `POST /api/send-director-results` returned `200 {"success":true}` for the same verification address, and Resend recorded outbound message `2c842d55-4da3-4fa5-a7d7-3fb6c49b2ed5` with last event `bounced` for the same reason.
- 2026-03-21: live `POST /api/referral/lead` returned `503 {"error":"Lead service not configured"}`, matching the disabled `referral-lead` entry in `docs/guides/PRODUCTION_ENV_CONTRACT.md`; this is intentionally non-blocking while the feature stays disabled on shipped surfaces.

## 4) Accessibility + Performance Spot Check (Deferrable With Notes)

- [~] Keyboard/focus sanity pass complete
- [~] Screen-reader/live-region sanity pass complete
- [~] Lighthouse desktop + mobile run captured
- [~] No major console/runtime regressions

Notes:
- Deferred for this release. Core site and key flows were manually exercised, but a formal accessibility/performance evidence pass was not captured in the release report.

## 5) Observability + Analytics (Deferrable With Notes Unless A Live Blocker Is Active)

- [~] Sentry release tagging and error volume sane
- [~] No repeating `calculation_anomaly` alerts
- [~] Key analytics funnel events observed
- [~] No obvious analytics PII leakage

Notes:
- 2026-03-11: Linear team query confirmed Sentry webhook automation is creating issues, but they are landing at the `PAYTAX` team level without project assignment.
- Auto-created Sentry issues currently visible in Linear include:
  - `PAYTAX-173` `🐛 Sentry: Error: feature named \`hover\` was not found`
  - `PAYTAX-172` `🐛 Sentry: Error: Calculation anomaly detected (primary)`
  - `PAYTAX-171` `🐛 Sentry: Error: Calculation anomaly detected (previous_year)`
  - `PAYTAX-170` `🐛 Sentry: TypeError: (0 , d.QP) is not a function`
  - `PAYTAX-169` `🐛 Sentry: Error: feature named \`performanceMetrics\` was not found`
  - `PAYTAX-168` `🐛 Sentry: Error: Connection closed.`
  - `PAYTAX-167` `🐛 Sentry: RangeError: Maximum call stack size exceeded.`
  - `PAYTAX-166` `🐛 Sentry: SecurityError: Failed to read the 'localStorage' property from 'Window': Access is denied for this document.`
  - `PAYTAX-165` `🐛 Sentry: Error: feature named \`webCompat\` was not found`
  - `PAYTAX-164` `🐛 Sentry: i: Failed to connect to MetaMask`
  - `PAYTAX-163` `🐛 Sentry: InvalidStateError: newestWorker is null`
  - `PAYTAX-162` `🐛 Sentry: TypeError: Script https://payetax.co.uk/sw.js load failed`
- Result: deferred from strict release blocking. Follow-up triage is still required for `PAYTAX-171` and `PAYTAX-172`.

## 6) SEO + Indexing (Deferrable With Notes)

- [~] `robots.txt` and `sitemap.xml` reachable
- [~] Canonical/metadata sanity on key pages
- [~] Structured data validation spot check completed

Notes:
- Deferred for this release. Recent audit work covered these areas, but a fresh release-specific evidence pass was not captured here.

## Final Decision

- [ ] Validation complete
- [ ] `Status` updated to `COMPLETE`
- [ ] Release notes/changelog link to this report

Decision Notes:
- 2026-03-21: still blocked from `COMPLETE`. The shipped newsletter subscribe flow is broken in production until the Kit uid/id fix is deployed, unsubscribe was not revalidated after the failed subscribe path, and the release report cannot be closed honestly until those checks pass on the live site.

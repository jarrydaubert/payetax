# Release Verification Report - v5.1.3

Date: 2026-06-02
Release Version: v5.1.3
Commit: b8fc33b3
Deployment URL: TBD
Release Notes URL: TBD
Releasing Engineer: Jarryd Aubert
Status: IN_PROGRESS

Reference checklist: `docs/guides/POST_RELEASE_VALIDATION.md`

Legend:
- `- [x]` complete
- `- [ ]` blocking and still open
- `- [~]` explicitly deferred/non-blocking with justification in `Notes`

---

## 0) Local Preflight

- [x] `bun run release:verify` passed
- [~] `bun run check:production-env-contract` passed against the intended Vercel Production project
- [x] Key output/caveats captured below

Notes:
- `bun run release:verify` passed. It ran `bun run check:repo`, `bun run audit:deps`, `bun run test:no-coverage`, `bun run build`, and `bun run release:report:status`.
- Local validation for the release branch also passed before final `release:verify`: `bun run check:repo`, `bun audit`, `bun run audit:deps`, `bun run test:no-coverage`, and `bun run build`.
- Version sync passed: `package.json` and `public/sw.js` cache names are both `v5.1.3`.
- Current-tree scans found no retired hosting references and no Linear API key-shaped literals outside `bun.lock`.
- Production validation remains deferred until the intended Vercel production env is configured.

## 1) Production Smoke

- [~] Homepage + calculator CTA path works
- [~] PAYE calculator works for at least two salaries
- [~] Director Intelligence flow completes
- [~] Blog index + one post loads cleanly

Notes:
- Deferred until the intended production project is configured and live.

## 2) Security + Abuse Controls

- [~] CSRF/origin enforcement verified
- [~] Rate limiting verified (distributed, not in-memory fallback)
  - Evidence: `bun run ops:verify-rate-limit` output captured below
- [~] Sentry webhook signature handling verified

Notes:
- Deferred until the intended Vercel production env is configured.

## 3) Email + Webhooks

- [~] PAYE results email route returns success through the configured transactional email provider
- [~] Director results email route returns success through the configured transactional email provider
- [~] Feedback form submits successfully and creates the intended operator notification

Notes:
- Deferred until the intended Vercel production env is configured.

## 4) Accessibility + Performance Spot Check (Deferrable With Notes)

- [~] Keyboard/focus sanity pass complete
- [~] Screen-reader/live-region sanity pass complete
- [~] Lighthouse desktop + mobile run captured
- [~] No major console/runtime regressions

Notes:
- Deferred until the intended Vercel production project is available.

## 5) Observability + Analytics (Deferrable With Notes Unless A Live Blocker Is Active)

- [~] Sentry release tagging and error volume sane
- [~] No repeating `calculation_anomaly` alerts
- [~] Key analytics funnel events observed
- [~] No obvious analytics PII leakage

Notes:
- Deferred until the intended Vercel production project is available.

## 6) SEO + Indexing (Deferrable With Notes)

- [~] `robots.txt` and `sitemap.xml` reachable
- [~] Canonical/metadata sanity on key pages
- [~] Structured data validation spot check completed

Notes:
- Deferred until the intended Vercel production project is available.

## Final Decision

- [~] Validation complete
- [~] `Status` updated to `COMPLETE`
- [~] Release notes/changelog link to this report

Decision Notes:
- Local release readiness is in progress and production validation is intentionally deferred until the intended production env is configured.

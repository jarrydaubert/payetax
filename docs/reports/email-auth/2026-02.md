# Email Auth Report — 2026-02

Checked on: February 20, 2026  
Checked by: Codex (initial baseline run)  
Domain: `payetax.co.uk`

---

## 1) DNS Record Checks

### SPF (`payetax.co.uk`)
- Status: `PASS`
- Observed:
  - `v=spf1 include:spf.protection.outlook.com -all`

### DMARC (`_dmarc.payetax.co.uk`)
- Status: `PASS` (record exists)
- Observed:
  - `v=DMARC1; p=none;`
- Risk note:
  - Policy is monitoring-only (`p=none`), not enforcement.

### DKIM — Resend (`resend._domainkey.payetax.co.uk`)
- Status: `PASS`
- Observed:
  - TXT record present with RSA public key (`p=...`).

### DKIM — additional common selectors
- `s1._domainkey.payetax.co.uk`: `FAIL` (NXDOMAIN)
- `s2._domainkey.payetax.co.uk`: `FAIL` (NXDOMAIN)
- `selector1._domainkey.payetax.co.uk`: `FAIL` (NXDOMAIN)
- `selector2._domainkey.payetax.co.uk`: `FAIL` (NXDOMAIN)
- Note:
  - These may be expected if those selectors are not configured providers. Treat as informational until matched against provider-required selector names.

---

## 2) Provider Dashboard Checks

### Resend domain auth
- Status: `TODO` (requires dashboard verification)
- Required:
  - Confirm domain shows verified/authenticated in Resend dashboard.

### Kit sending-domain auth
- Status: `TODO` (requires dashboard verification)
- Required:
  - Confirm whether Kit sends from `@payetax.co.uk` and, if so, verify expected DKIM/SPF selector records from Kit dashboard.

---

## 3) Risk Summary

- Current baseline confirms root SPF, DMARC record presence, and Resend DKIM selector.
- DMARC policy is currently non-enforcing (`p=none`) and should be reviewed for phased hardening once alignment is stable.
- Provider dashboard confirmation remains outstanding for this baseline.

---

## 4) Remediation / Follow-ups

1. Verify Resend domain-auth status in dashboard and update this report.
   - Owner: releasing engineer
   - Target: next release validation window

2. Verify Kit sending-domain selectors from dashboard and reconcile expected DKIM records.
   - Owner: growth/content owner
   - Target: next monthly check

3. Propose DMARC hardening plan (`p=none` -> `quarantine` staged rollout) after two clean monthly checks.
   - Owner: releasing engineer
   - Target: March 2026 report
